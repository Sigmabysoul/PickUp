DATABASE_URL=postgres://pickup:pickup_local_only@127.0.0.1:55432/pickup
# PickUp Production Environment Variables
NODE_ENV=production
PORT=3001

# Database Configuration (PostgreSQL 16)
# If using Docker Compose (docker-compose.prod.yml):
POSTGRES_DB=pickup
POSTGRES_USER=pickup
POSTGRES_PASSWORD=your_secure_password_here

# If using Managed Cloud PostgreSQL (Render, Railway, Supabase, Neon, AWS RDS):
# DATABASE_URL=postgres://user:password@host:5432/dbname?sslmode=require
